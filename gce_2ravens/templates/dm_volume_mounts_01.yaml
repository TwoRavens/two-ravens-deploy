      # ----------------------------------
      # shared between containers
      # ref: dm_volume_mounts_01.yaml
      # ----------------------------------
      - name: ravens-volume
        mountPath: /ravens_volume
        subPath: 2ravens_org{{ serviceNameSuffix}}
        readOnly: false
      # ----------------------------------
